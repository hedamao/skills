# Claude Code Collaboration Rules

## Agent Team Requirement Analysis Protocol

当接到一个新的需求、缺陷修复、优化项或架构调整任务时，不要直接开始编码，必须先从系统全局视角组织一次 `agent team` 方案评审。

### 目标
- 从前端、BFF、底层核心系统三个层面统一分析影响范围
- 给出端到端的技术方案，而不是只回答单点实现
- 明确跨层接口、数据流、依赖关系、风险和验证方案

### Team 组建规则
接到需求后，必须至少覆盖以下 5 个角色视角；如果运行环境支持 subagents，则优先委派对应 agent；如果不支持，则主智能体必须按这些角色分段完成分析：

1. `SystemArchitect`
   - 负责整体架构、系统边界、模块拆分、核心流程和非功能性影响评估
2. `VueFrontendDeveloper`
   - 负责前端页面、交互、状态管理、接口消费、兼容性和用户体验影响分析
3. `BFF Owner`
   - 负责聚合接口、鉴权、接口编排、字段适配、错误码、灰度兼容和前后端契约设计
4. `Core System Owner`
   - 负责领域模型、核心服务、数据库/缓存/消息、事务一致性、性能和稳定性影响分析
5. `QATester`
   - 负责测试策略、回归范围、上线验证、监控告警和回滚建议

说明：
- 如果仓库中没有显式的 `BFF` 或 `Core` subagent，则由 `SystemArchitect` + `FullstackDeveloper` 补位完成这两个视角的分析。
- 任何一层如果判断为“无需改动”，必须明确写出“无需改动 + 原因”，不能省略。

### 工作流程
1. 先复述需求，并识别业务目标、边界、约束和待确认问题
2. 组建 `agent team`，分别从前端、BFF、核心系统进行影响分析
3. 汇总跨层方案，确保接口契约、数据结构、时序流程和异常处理一致
4. 输出综合技术方案
5. 在用户确认方案前，不要直接进入大规模编码

### 综合技术方案输出模板
每次都按以下结构输出：

#### 1. 需求理解
- 业务目标
- 成功标准
- 范围边界
- 前置假设

#### 2. Agent Team 结论
- 前端视角：页面/组件/状态/API 变更点、兼容性影响、交互调整
- BFF 视角：接口设计、字段映射、鉴权校验、编排逻辑、错误处理
- 核心系统视角：领域服务、数据库、缓存、消息、事务、性能、稳定性
- 架构视角：跨层依赖、时序、扩展性、灰度发布与兼容策略
- 测试视角：单测、集成测试、联调、回归、监控与回滚方案

#### 3. 端到端技术方案
- 调用链路
- 数据流转
- 接口契约变更
- 数据模型/表结构变更
- 配置或基础设施变更

#### 4. 风险与权衡
- 技术风险
- 业务风险
- 性能风险
- 兼容性风险
- 备选方案与取舍理由

#### 5. 落地计划
- 建议实施步骤
- 联调顺序
- 发布顺序
- 验证清单

#### 6. 待确认问题
- 列出所有阻塞实施或影响方案选择的关键问题

### 输出要求
- 方案必须体现“前端 -> BFF -> 核心系统”的完整链路
- 必须明确哪些模块改动、哪些接口改动、哪些数据结构改动
- 必须写出兼容策略，包括旧接口兼容、字段兼容、灰度/回滚方案
- 必须写出测试与上线验证方案
- 如果信息不足，先列出缺失信息和假设，再继续给出基于假设的方案
- 除非用户明确要求直接编码，否则优先输出综合方案并等待确认

### 禁止事项
- 禁止只从单一层面回答问题
- 禁止跳过 BFF 或核心系统影响分析
- 禁止没有接口契约和数据流说明就直接开始实现
- 禁止忽略异常处理、监控告警、回滚策略
